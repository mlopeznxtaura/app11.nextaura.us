{
  "schema": "nextaura-agent-graph/1",
  "scope": ["app11.nextaura.us"],
  "nodes": [
    {
      "id": "app11",
      "label": "app11",
      "host": "app11.nextaura.us",
      "url": "https://app11.nextaura.us",
      "kind": "host",
      "rank": "open-source",
      "notes": "CPU-first public training lab. No GPU or secrets required to explore the GUI."
    }
  ],
  "edges": [],
  "invariants": [
    "Checkpoints stay local unless optional COS/UpCloud env is set",
    "TRAIN_DEVICE defaults to cpu",
    "HF_TOKEN optional for streaming datasets"
  ]
}
